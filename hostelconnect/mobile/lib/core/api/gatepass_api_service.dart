/// Gate Pass API Service
/// Handles gate pass-related API calls
class GatePassApiService {
  /// Record gate scan event
  Future<void> recordGateScanEvent(Map<String, dynamic> request) async {
    // Mock implementation
    await Future.delayed(const Duration(milliseconds: 400));
    // In real implementation, make HTTP request to backend
  }
}