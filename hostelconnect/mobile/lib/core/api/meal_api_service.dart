/// Meal API Service
/// Handles meal-related API calls
class MealApiService {
  /// Submit meal intent
  Future<void> submitMealIntent(Map<String, dynamic> request) async {
    // Mock implementation
    await Future.delayed(const Duration(milliseconds: 350));
    // In real implementation, make HTTP request to backend
  }
}